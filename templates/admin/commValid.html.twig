{% extends "base.html.twig" %}

{% block body %}

    <div class ="bannierre"> 
        <h1 class="bannierre-titre"> Validation des commentaires </h1>
    </div>
<div class="container mt-5 mb-5 bloc-form">

    <table class="table table-striped">
        <thead>
            <tr class="text-center">
                <th>Utilisateur</th>
                <th>Le commentaire</th>
                <th>Numéro de fiche</th>
                <th>Valider</th>
                <th>Supprimer</th>
            </tr>
        </thead>
        <tbody>
        
        {% for comm in lesCommentaires %}
           <tr class="text-center">
            <td class="text-tableau">{{ comm['pseudo'] }}</td>
            <td class="text-tableau">{{ comm['texte'] }}</td>
            <td class="text-tableau">{{ comm['ficheId'] }}</td>
            <td><a href="/admin/validCommentaire/{{ comm['id'] }}" class="btn btn-success">Valider</a></td>
            <td><a href="/admin/suppCommentaire/{{ comm['id'] }}" class="btn btn-danger">Supprimer</a></td>
        
        </tr>
        {% endfor %}
        
        </tbody>
    </table>
</div>
<br><br><br><br>
{% endblock %}
